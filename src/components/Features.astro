---
const features = [
  {
    icon: "fingerprint",
    title: "Your visual identity, preserved",
    description: "The AI doesn't just make renders—it makes your renders. Trained specifically on your body of work to maintain brand consistency."
  },
  {
    icon: "zap",
    title: "From sketch to render in seconds",
    description: "What used to take hours or days now happens in moments. Focus on design, not on wrestling with rendering software."
  },
  {
    icon: "refresh",
    title: "Iterate without limits",
    description: "Don't like the lighting? Want a different angle? Regenerate variations instantly. Explore options you never had time for."
  },
  {
    icon: "lock",
    title: "Your work stays yours",
    description: "Your projects are never used to train models for others. Your creative DNA is protected and private to your firm."
  }
];

// Simple SVG icons
const icons = {
  fingerprint: "M12 11c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm6 6c0-3.31-2.69-6-6-6s-6 2.69-6 6",
  zap: "M13 2L3 14h8l-1 8 10-12h-8l1-8z",
  refresh: "M1 4v6h6M23 20v-6h-6M20.49 9A9 9 0 005.64 5.64L1 10m22 4l-4.64 4.36A9 9 0 013.51 15",
  lock: "M19 11H5c-1.1 0-2 .9-2 2v7c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2v-7c0-1.1-.9-2-2-2zM7 11V7c0-2.76 2.24-5 5-5s5 2.24 5 5v4"
};
---

<section id="features" class="py-24 lg:py-32">
  <div class="max-w-7xl mx-auto px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-16 items-start">
      <!-- Left: Headline -->
      <div class="lg:sticky lg:top-32">
        <p class="text-sm font-medium tracking-widest uppercase text-muted-foreground mb-4">Features</p>
        <h2 class="text-3xl sm:text-4xl font-serif tracking-tight mb-6">
          Built for the way architects actually work
        </h2>
        <p class="text-lg text-muted-foreground leading-relaxed">
          We talked to over 100 firms before writing a single line of code. Every feature exists because you asked
          for it.
        </p>
      </div>

      <!-- Right: Feature Grid -->
      <div class="grid gap-8">
        {features.map((feature) => (
          <div class="group p-6 rounded-lg border border-border bg-card hover:bg-secondary/50 transition-colors">
            <div class="flex gap-5">
              <div class="shrink-0 w-10 h-10 rounded-md bg-secondary flex items-center justify-center group-hover:bg-accent transition-colors">
                <svg class="w-5 h-5 text-foreground" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d={icons[feature.icon]} />
                </svg>
              </div>
              <div>
                <h3 class="font-medium mb-2">{feature.title}</h3>
                <p class="text-muted-foreground text-sm leading-relaxed">{feature.description}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
