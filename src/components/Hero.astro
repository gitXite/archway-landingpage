---
---

<section class="pt-32 pb-20 lg:pt-40 lg:pb-32" id="hero">
  <div class="max-w-7xl mx-auto px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      <div class="space-y-8">
        <div class="space-y-6">
          <p class="text-sm font-medium tracking-widest uppercase text-muted-foreground">For Arkitekter</p>
          <h1 class="text-4xl sm:text-5xl lg:text-6xl font-serif tracking-tight text-balance leading-[1.1]">
            Renderinger for <span class="font-bold bg-linear-to-r from-[#ff9a4d] via-[#ff5c5c] to-[#e91e63] bg-clip-text text-transparent">ditt kontor</span>
          </h1>
          <p class="text-lg text-muted-foreground max-w-lg leading-relaxed">
            Last opp en tegning og en referanse, og generer imponerende renderinger på sekunder. 
            AI modellen trenes opp på kontorets prosjekter for å matche deres stil og identitet. 
            Helt uten prompting. 
          </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-4">
          <a class="cursor-pointer inline-flex items-center justify-center gap-2 rounded-md bg-primary px-8 py-3 text-base font-medium text-primary-foreground hover:bg-primary/90 transition-colors group">
            Kom i gang
            <svg class="w-4 h-4 group-hover:translate-x-1.5 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
            </svg>
          </a>
          <a href="/priser" class="cursor-pointer inline-flex items-center justify-center rounded-md border border-border bg-transparent px-8 py-3 text-base font-medium hover:bg-secondary transition-colors">
            Se priser
          </a>
        </div>

        <div class="pt-4 flex items-center gap-4 text-sm text-muted-foreground">
          <div class="flex -space-x-2">
            {[1, 2, 3, 4].map(() => (
              <div class="w-8 h-8 rounded-full bg-secondary border-2 border-background" />
            ))}
          </div>
          <span>Anbefalt av flere arkitektfirmaer</span>
        </div>
      </div>

      <div class="relative">
        <div class="aspect-4/3 bg-secondary rounded-lg overflow-hidden relative">
          <div class="absolute inset-0 flex">
            <div class="w-1/2 bg-card border-r border-border p-4 flex flex-col justify-center">
              <span class="text-xs font-medium text-muted-foreground mb-2">Input</span>
              <div class="flex flex-1 bg-secondary rounded border border-dashed border-border justify-center">
                <img class="" src="/line-drawing.webp" alt="line drawing">
              </div>
            </div>
            <div class="w-1/2 p-4 flex flex-col justify-center">
              <span class="text-xs font-medium text-muted-foreground mb-2">Output</span>
              <div class="flex justify-center flex-1 rounded overflow-hidden bg-linear-to-br from-muted to-accent/20">
                <img class="" src="/render.webp" alt="render">
              </div>
            </div>
          </div>
        </div>

        <div class="absolute -bottom-4 left-1/2 -translate-x-1/2 bg-card shadow-lg rounded-full px-4 py-2 border border-border">
          <span class="text-sm font-medium">Generert på <span class="text-accent">30s</span></span>
        </div>
      </div>
    </div>
  </div>
</section>
